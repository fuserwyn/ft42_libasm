# Libasm - Чек-лист соответствия заданию

Данный документ содержит полный чек-лист для проверки соответствия проекта требованиям задания.

---

## 📋 Общие инструкции (Chapter II)

### Обязательные требования

- [x] **Функции не падают неожиданно** (segmentation fault, bus error, double free, etc.)
  - Все функции протестированы и работают корректно
  - Обработка ошибок реализована для syscalls

- [x] **Makefile содержит правила**: `$(NAME)`, `all`, `clean`, `fclean`, `re`
  - `$(NAME)` = `libasm.a`
  - `all`: собирает обязательные функции
  - `clean`: удаляет объектные файлы
  - `fclean`: удаляет все собранные файлы
  - `re`: пересобирает проект
  - Makefile перекомпилирует только изменённые файлы

- [x] **Правило bonus в Makefile**
  - Реализовано правило `bonus`, которое включает бонусные функции
  - Бонусы в отдельных файлах `*_bonus.s`

- [x] **Тестовая программа main**
  - Реализована в `tests/main.c`
  - Тестирует все функции (mandatory + bonus)

- [x] **64-bit assembly**
  - Используется формат `elf64` (Linux) / `macho64` (macOS)
  - 64-битная архитектура x86-64

- [x] **Отдельные файлы .s (не inline ASM)**
  - Все функции в отдельных `.s` файлах
  - Не используется inline assembly

- [x] **Компиляция через nasm**
  - Используется компилятор `nasm`
  - Команда: `nasm -f elf64/macho64 ...`

- [x] **Intel syntax (не AT&T)**
  - Используется Intel syntax: `mov dst, src`
  - Не используется AT&T: `mov %src, %dst`

- [x] **Запрещённый флаг `-no-pie` не используется**
  - Проверено: флаг отсутствует в Makefile

---

## 🔧 Обязательная часть (Chapter III)

### Требования к библиотеке

- [x] **Библиотека называется `libasm.a`**
  - Имя библиотеки: `libasm.a`
  - Создаётся через `ar rcs libasm.a ...`

- [x] **Функция main для тестирования**
  - Файл: `tests/main.c`
  - Компилируется вместе с библиотекой
  - Демонстрирует работоспособность всех функций

### Обязательные функции

- [x] **ft_strlen** (man 3 strlen)
  - Файл: `src/ft_strlen.s`
  - Реализована корректно
  - Возвращает `size_t` (длину строки)

- [x] **ft_strcpy** (man 3 strcpy)
  - Файл: `src/ft_strcpy.s`
  - Реализована корректно
  - Копирует строку включая `\0`
  - Возвращает указатель на `dst`

- [x] **ft_strcmp** (man 3 strcmp)
  - Файл: `src/ft_strcmp.s`
  - Реализована корректно
  - Возвращает разницу между строками

- [x] **ft_write** (man 2 write)
  - Файл: `src/ft_write.s`
  - Реализована корректно
  - Использует системный вызов `write`
  - Обрабатывает ошибки и устанавливает `errno`

- [x] **ft_read** (man 2 read)
  - Файл: `src/ft_read.s`
  - Реализована корректно
  - Использует системный вызов `read`
  - Обрабатывает ошибки и устанавливает `errno`

- [x] **ft_strdup** (man 3 strdup)
  - Файл: `src/ft_strdup.s`
  - Реализована корректно
  - Вызывает `malloc` для выделения памяти
  - Использует `ft_strlen` и `ft_strcpy`

### Обработка ошибок syscalls

- [x] **Проверка ошибок в syscalls**
  - `ft_write`: проверяет ошибки после `syscall`
  - `ft_read`: проверяет ошибки после `syscall`

- [x] **Установка переменной errno**
  - Linux: используется `__errno_location()`
  - macOS: используется `___error()`
  - `errno` устанавливается корректно при ошибках

---

## 🎁 Бонусная часть (Chapter IV)

### Структура t_list

- [x] **Структура соответствует заданию**
  ```c
  typedef struct s_list
  {
      void            *data;
      struct s_list   *next;
  }   t_list;
  ```
  - Определена в `include/libasm.h`

### Бонусные функции

- [x] **ft_atoi_base** (Annex V.1)
  - Файл: `src/bonus/ft_atoi_base_bonus.s`
  - Конвертирует строку в число с заданной системой счисления
  - Проверяет валидность базы (пустая, дубликаты, недопустимые символы)
  - Обрабатывает знаки `+` и `-`
  - Пропускает пробелы

- [x] **ft_list_push_front** (Annex V.2)
  - Файл: `src/bonus/ft_list_push_front_bonus.s`
  - Добавляет элемент в начало списка
  - Выделяет память через `malloc`
  - Обновляет указатель на начало списка

- [x] **ft_list_size** (Annex V.3)
  - Файл: `src/bonus/ft_list_size_bonus.s`
  - Возвращает количество элементов в списке
  - Возвращает 0 для `NULL`

- [x] **ft_list_sort** (Annex V.4)
  - Файл: `src/bonus/ft_list_sort_bonus.s`
  - Сортирует список по возрастанию
  - Использует функцию сравнения `cmp`
  - Сохраняет указатель на функцию в безопасном регистре

- [x] **ft_list_remove_if** (Annex V.5)
  - Файл: `src/bonus/ft_list_remove_if_bonus.s`
  - Удаляет элементы, соответствующие `data_ref`
  - Использует `cmp` для сравнения
  - Освобождает данные через `free_fct`
  - Освобождает узлы через `free`
  - Правильно обновляет указатели

### Бонусные файлы

- [x] **Все бонусы в файлах `*_bonus.s`**
  - `ft_atoi_base_bonus.s`
  - `ft_list_push_front_bonus.s`
  - `ft_list_size_bonus.s`
  - `ft_list_sort_bonus.s`
  - `ft_list_remove_if_bonus.s`

---

## 📝 Проверка соответствия Calling Convention x86-64

- [x] **Аргументы передаются в регистрах**
  - Первый аргумент: `rdi`
  - Второй аргумент: `rsi`
  - Третий аргумент: `rdx`
  - Четвёртый аргумент: `rcx`
  - Пятый аргумент: `r8`
  - Шестой аргумент: `r9`

- [x] **Возвращаемое значение в `rax`**
  - Все функции возвращают результат в `rax`

- [x] **Сохранение регистров**
  - Регистры `rbx`, `rbp`, `r12-r15` сохраняются при использовании
  - Используется `push`/`pop` для сохранения

- [x] **Выравнивание стека**
  - Стек выровнен на 16 байт перед вызовами функций

---

## 🔍 Проверка системных вызовов

### ft_write

- [x] **Правильный номер системного вызова**
  - Linux: `1` (SYS_write)
  - macOS: `0x2000004` (SYS_write с offset)

- [x] **Правильные аргументы**
  - `rdi`: file descriptor
  - `rsi`: buffer pointer
  - `rdx`: count

- [x] **Обработка ошибок**
  - Проверка возвращаемого значения
  - Установка `errno` при ошибке

### ft_read

- [x] **Правильный номер системного вызова**
  - Linux: `0` (SYS_read)
  - macOS: `0x2000003` (SYS_read с offset)

- [x] **Правильные аргументы**
  - `rdi`: file descriptor
  - `rsi`: buffer pointer
  - `rdx`: count

- [x] **Обработка ошибок**
  - Проверка возвращаемого значения
  - Установка `errno` при ошибке

---

## ✅ Финальная проверка

### Компиляция и сборка

- [x] **Чистая сборка**
  ```bash
  make fclean
  make bonus
  ```
  - Сборка проходит без ошибок и предупреждений

- [x] **Тестирование**
  ```bash
  make test
  ```
  - Все тесты проходят успешно

- [x] **Проверка структуры проекта**
  - Все файлы на месте
  - Правильные имена файлов
  - Правильная структура директорий

### Структура файлов

```
ft42_libasm/
├── include/
│   └── libasm.h          ✅
├── src/
│   ├── ft_strlen.s       ✅
│   ├── ft_strcpy.s       ✅
│   ├── ft_strcmp.s       ✅
│   ├── ft_write.s        ✅
│   ├── ft_read.s         ✅
│   ├── ft_strdup.s       ✅
│   └── bonus/
│       ├── ft_atoi_base_bonus.s        ✅
│       ├── ft_list_push_front_bonus.s  ✅
│       ├── ft_list_size_bonus.s        ✅
│       ├── ft_list_sort_bonus.s        ✅
│       └── ft_list_remove_if_bonus.s   ✅
├── tests/
│   └── main.c            ✅
├── Makefile              ✅
└── libasm.a              ✅ (после сборки)
```

---

## 📊 Итоговая оценка

### Обязательная часть
- ✅ Все требования выполнены
- ✅ Все функции работают корректно
- ✅ Обработка ошибок реализована
- ✅ Makefile соответствует требованиям

### Бонусная часть
- ✅ Все 5 бонусных функций реализованы
- ✅ Файлы правильно названы `*_bonus.s`
- ✅ Структура `t_list` соответствует заданию
- ✅ Все функции протестированы

### Дополнительные улучшения
- ✅ Кроссплатформенная поддержка (macOS/Linux)
- ✅ Полное тестирование всех функций
- ✅ Подробная документация (README)

---

## 🎯 Статус: **ГОТОВО К ЗАЩИТЕ**

Все требования задания выполнены. Проект готов к отправке и защите.

---

*Последнее обновление: на основе subject v5.4*

