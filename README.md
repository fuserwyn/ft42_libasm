# Libasm - Руководство по защите проекта

## 📋 Содержание
1. [Общая информация](#общая-информация)
2. [Подготовка к защите](#подготовка-к-защите)
3. [Обязательные функции (Mandatory)](#обязательные-функции-mandatory)
4. [Бонусные функции](#бонусные-функции)
5. [Типичные вопросы на защите](#типичные-вопросы-на-защите)
6. [Демонстрация работы](#демонстрация-работы)
7. [Частые ошибки](#частые-ошибки)
8. [Полезные команды](#полезные-команды)

---

## Общая информация

**Libasm** - это проект, в котором вы реализуете стандартные функции языка C на языке ассемблера x86-64 (NASM).

### Структура проекта
```
ft42_libasm/
├── include/
│   └── libasm.h          # Заголовочный файл с прототипами
├── src/
│   ├── ft_strlen.s       # Обязательные функции
│   ├── ft_strcpy.s
│   ├── ft_strcmp.s
│   ├── ft_write.s
│   ├── ft_read.s
│   ├── ft_strdup.s
│   └── bonus/            # Бонусные функции
│       ├── ft_atoi_base_bonus.s
│       ├── ft_list_push_front_bonus.s
│       ├── ft_list_size_bonus.s
│       ├── ft_list_sort_bonus.s
│       └── ft_list_remove_if_bonus.s
├── tests/
│   └── main.c            # Тестовый файл
├── Makefile
└── README.md
```

### Команды сборки
```bash
make          # Собрать обязательные функции
make bonus    # Собрать все функции (mandatory + bonus)
make test     # Собрать и запустить тесты
make clean    # Удалить объектные файлы
make fclean   # Удалить все собранные файлы
make re       # Пересобрать проект
```

### Совместимость с платформами

Проект поддерживает как **macOS**, так и **Linux (Ubuntu)**:
- **macOS**: Использует формат `macho64`, системные вызовы со смещением `0x2000000`, имена функций с префиксом подчёркивания
- **Linux**: Использует формат `elf64`, прямые номера системных вызовов, имена функций без префикса подчёркивания
- Makefile автоматически определяет платформу и использует соответствующие флаги компиляции
- Все исходные файлы используют условную компиляцию (`%ifidn __OUTPUT_FORMAT__,elf64`) для поддержки обеих платформ

---

## Подготовка к защите

### ✅ Чек-лист перед защитой

1. **Проверка чистой сборки**
   ```bash
   make fclean
   make bonus
   ```
   - Убедитесь, что проект собирается без ошибок и предупреждений
   - Проверьте, что `libasm.a` создан успешно
   - Убедитесь, что все объектные файлы находятся в директории `build/`

2. **Запуск тестов**
   ```bash
   make test
   ```
   - Все тесты должны проходить успешно
   - Протестируйте как обязательные, так и бонусные функции
   - Проверьте граничные случаи (NULL, пустые строки и т.д.)

3. **Кросс-платформенное тестирование (если возможно)**
   ```bash
   # Тест на macOS
   make fclean && make bonus && make test
   
   # Тест на Linux (Ubuntu)
   make fclean && make bonus && make test
   ```

4. **Проверка нормы**
   - Убедитесь, что все файлы соответствуют норме 42
   - Проверьте форматирование и комментарии
   - Убедитесь, что нет `.o` файлов в репозитории (проверьте `.gitignore`)

5. **Изучение кода**
   - Понимайте каждую строку ассемблерного кода
   - Знайте назначение каждого регистра
   - Понимайте calling convention x86-64
   - Будьте готовы объяснить выбор алгоритма

6. **Подготовка демонстрации**
   - Подготовьте простые примеры для каждой функции
   - Будьте готовы пройтись по коду построчно
   - Подготовьте ответы на типичные вопросы (см. ниже)

---

## Обязательные функции (Mandatory)

### 1. `ft_strlen`

**Прототип:** `size_t ft_strlen(const char *str);`

**Что делает:** Вычисляет длину строки (количество символов до `\0`).

**Ключевые моменты для защиты:**
- Использует регистр `rdi` для аргумента (первый аргумент в x86-64)
- Использует `rax` для возвращаемого значения
- Использует `xor rax, rax` для обнуления (быстрее, чем `mov rax, 0`)
- Проверяет каждый байт через `cmp byte [rdi + rax], 0`
- Возвращает длину в `rax`

**Возможные вопросы:**
- Почему используется `xor rax, rax` вместо `mov rax, 0`?
- Что произойдет, если передать `NULL`? (не обрабатывается - это нормально для strlen)
- Почему используется `byte` в `cmp byte [rdi + rax], 0`?

**Ответы:**
- `xor rax, rax` - это оптимизация: команда короче и быстрее
- `strlen` в стандартной библиотеке также не обрабатывает `NULL` (undefined behavior)
- `byte` указывает размер операнда (1 байт), так как мы работаем с символами

---

### 2. `ft_strcpy`

**Прототип:** `char *ft_strcpy(char *dst, const char *src);`

**Что делает:** Копирует строку из `src` в `dst`, включая завершающий `\0`.

**Ключевые моменты:**
- Сохраняет указатель на `dst` в `rax` для возврата
- Использует `rdi` для `dst`, `rsi` для `src`
- Копирует байт за байтом
- Использует `test dl, dl` для проверки на `\0` (быстрее, чем `cmp`)
- Копирует завершающий `\0`

**Возможные вопросы:**
- Почему функция возвращает указатель на `dst`?
- Что произойдет при перекрытии буферов? (undefined behavior, как в стандартной `strcpy`)
- Почему используется `test dl, dl` вместо `cmp dl, 0`?

**Ответы:**
- Возврат указателя позволяет использовать функцию в выражениях: `ft_strcpy(dst, src) + 5`
- Перекрытие буферов не обрабатывается (как в стандартной библиотеке)
- `test` быстрее, так как выполняет побитовую операцию AND без записи результата

---

### 3. `ft_strcmp`

**Прототип:** `int ft_strcmp(const char *s1, const char *s2);`

**Что делает:** Сравнивает две строки лексикографически.

**Ключевые моменты:**
- Возвращает отрицательное число, если `s1 < s2`
- Возвращает 0, если `s1 == s2`
- Возвращает положительное число, если `s1 > s2`
- Сравнивает байт за байтом
- Возвращает разницу между символами при первом различии

**Возможные вопросы:**
- Как вычисляется возвращаемое значение?
- Что произойдет, если одна из строк короче другой?
- Почему возвращается разница символов, а не просто -1/0/1?

**Ответы:**
- Возвращается `(unsigned char)s1[i] - (unsigned char)s2[i]` при первом различии
- Если одна строка короче, сравнение дойдет до `\0` и вернется разница
- Стандартная `strcmp` также возвращает разницу, а не фиксированные значения

---

### 4. `ft_write`

**Прототип:** `ssize_t ft_write(int fd, const void *buf, size_t count);`

**Что делает:** Записывает `count` байтов из буфера `buf` в файловый дескриптор `fd`.

**Ключевые моменты:**
- Использует системный вызов `write` (номер 0x2000004 на macOS, 1 на Linux)
- `rdi` = fd, `rsi` = buf, `rdx` = count
- Возвращает количество записанных байтов или -1 при ошибке
- Устанавливает `errno` при ошибке

**Возможные вопросы:**
- Какой номер системного вызова используется?
- Что такое файловый дескриптор?
- Как обрабатываются ошибки?

**Ответы:**
- На macOS: `0x2000004` (SYS_write), на Linux: `1`
- Файловый дескриптор - это число, идентифицирующее открытый файл (0=stdin, 1=stdout, 2=stderr)
- При ошибке `rax` содержит отрицательное значение, и устанавливается `errno`

**Важно:** 
- На macOS: системные вызовы имеют смещение `0x2000000` (write = `0x2000004`, read = `0x2000003`)
- На Linux: прямые номера системных вызовов (write = `1`, read = `0`)
- Обработка ошибок: macOS использует `___error()`, Linux использует `__errno_location()`
- Код использует условную компиляцию для поддержки обеих платформ

---

### 5. `ft_read`

**Прототип:** `ssize_t ft_read(int fd, void *buf, size_t count);`

**Что делает:** Читает до `count` байтов из файлового дескриптора `fd` в буфер `buf`.

**Ключевые моменты:**
- Использует системный вызов `read`
- На macOS: `0x2000003`, на Linux: `0`
- Возвращает количество прочитанных байтов (может быть меньше `count`)
- Возвращает 0 при достижении конца файла
- Возвращает -1 при ошибке

**Возможные вопросы:**
- Почему может быть прочитано меньше байтов, чем запрошено?
- Что происходит при чтении из stdin?
- Как обрабатывается конец файла?

**Ответы:**
- Системный вызов может вернуть меньше байтов из-за буферизации или доступных данных
- При чтении из stdin (fd=0) функция ждет ввода пользователя
- Конец файла определяется возвратом 0 байтов

---

### 6. `ft_strdup`

**Прототип:** `char *ft_strdup(const char *s);`

**Что делает:** Выделяет память и копирует строку.

**Ключевые моменты:**
- Вызывает `malloc` для выделения памяти
- Использует `ft_strlen` для определения размера
- Использует `ft_strcpy` для копирования
- Возвращает `NULL` при ошибке выделения памяти
- Выделяет `strlen(s) + 1` байтов (для `\0`)

**Ключевые моменты:**
- Сохраняет `rdi` в стек перед вызовом `ft_strlen`
- Восстанавливает исходную строку из стека для `ft_strcpy`
- Правильно обрабатывает случай `malloc` возвращает `NULL`

**Возможные вопросы:**
- Почему нужно сохранять `rdi` в стек?
- Что произойдет, если `malloc` вернет `NULL`?
- Почему выделяется `strlen(s) + 1` байтов?

**Ответы:**
- `rdi` изменяется при вызове `ft_strlen`, поэтому нужно сохранить исходное значение
- При `NULL` функция возвращает `NULL` (как стандартная `strdup`)
- `+1` нужен для завершающего нулевого байта `\0`

---

## Бонусные функции

### 1. `ft_atoi_base`

**Прототип:** `int ft_atoi_base(char *str, char *base);`

**Что делает:** Преобразует строку в число в указанной системе счисления.

**Ключевые моменты:**
- Обрабатывает знаки `+` и `-`
- Пропускает пробелы в начале
- Поддерживает любую систему счисления (2-16 и более)
- Проверяет валидность базы (не пустая, не содержит дубликатов, не содержит `+`/`-`)
- Возвращает 0 при ошибке

**Возможные вопросы:**
- Как определяется система счисления?
- Что произойдет при переполнении?
- Как обрабатываются недопустимые символы?

**Ответы:**
- Система счисления определяется длиной строки `base`
- Переполнение не обрабатывается явно (как в стандартной `atoi`)
- Недопустимые символы приводят к остановке парсинга

---

### 2. `ft_list_push_front`

**Прототип:** `void ft_list_push_front(t_list **begin_list, void *data);`

**Что делает:** Добавляет новый элемент в начало списка.

**Ключевые моменты:**
- Выделяет память для нового элемента через `malloc`
- Устанавливает `data` и `next`
- Обновляет `*begin_list` на новый элемент
- Обрабатывает случай пустого списка

**Возможные вопросы:**
- Что такое двойной указатель `**begin_list`?
- Как обрабатывается выделение памяти?
- Что произойдет, если `malloc` вернет `NULL`?

**Ответы:**
- Двойной указатель нужен для изменения указателя на начало списка
- При ошибке `malloc` элемент не добавляется (можно добавить обработку ошибок)
- Нужно проверить результат `malloc`

---

### 3. `ft_list_size`

**Прототип:** `int ft_list_size(t_list *begin_list);`

**Что делает:** Подсчитывает количество элементов в списке.

**Ключевые моменты:**
- Проходит по списку, считая элементы
- Возвращает 0 для `NULL`
- Использует простой цикл

**Возможные вопросы:**
- Какова сложность алгоритма?
- Что произойдет при циклическом списке?

**Ответы:**
- O(n) - линейная сложность
- Циклический список приведет к бесконечному циклу (не обрабатывается)

---

### 4. `ft_list_sort`

**Прототип:** `void ft_list_sort(t_list **begin_list, int (*cmp)());`

**Что делает:** Сортирует список с помощью функции сравнения.

**Ключевые моменты:**
- Использует алгоритм пузырьковой сортировки
- Сортирует данные (data), а не узлы
- Использует функцию сравнения `cmp`
- Сохраняет указатель на функцию сравнения в регистре (например, `r13`)

**Ключевые моменты реализации:**
- Сохраняет указатель на `cmp` в безопасном регистре (`r13`)
- Правильно сохраняет/восстанавливает регистры при вызове `cmp`
- Использует флаг `swapped` для оптимизации

**Возможные вопросы:**
- Почему используется пузырьковая сортировка?
- Как работает функция сравнения?
- Почему нужно сохранять указатель на функцию?

**Ответы:**
- Пузырьковая сортировка проста в реализации на ассемблере
- `cmp` возвращает >0 если первый элемент больше, <0 если меньше, 0 если равны
- Указатель на функцию нужно сохранить, так как регистры изменяются при вызове

---

### 5. `ft_list_remove_if`

**Прототип:** `void ft_list_remove_if(t_list **begin_list, void *data_ref, int (*cmp)(), void (*free_fct)(void *));`

**Что делает:** Удаляет элементы из списка, если они соответствуют `data_ref` по функции `cmp`.

**Ключевые моменты:**
- Проходит по списку
- Использует `cmp` для сравнения
- Вызывает `free_fct` для освобождения данных
- Освобождает память узла через `free`
- Правильно обновляет указатели

**Возможные вопросы:**
- Как обрабатывается удаление первого элемента?
- Что произойдет, если `free_fct` равен `NULL`?
- Как обновляются указатели при удалении?

**Ответы:**
- Удаление первого элемента требует обновления `*begin_list`
- Если `free_fct == NULL`, данные не освобождаются
- Нужно сохранить указатель на следующий элемент перед удалением

---

## Типичные вопросы на защите

### Общие вопросы об ассемблере

1. **Что такое calling convention x86-64?**
   - Первые 6 целочисленных аргументов передаются в регистрах: `rdi`, `rsi`, `rdx`, `rcx`, `r8`, `r9`
   - Возвращаемое значение в `rax`
   - Регистры `rbx`, `rbp`, `r12-r15` должны сохраняться вызываемой функцией
   - Стек должен быть выровнен на 16 байт перед вызовом функций

2. **Какие регистры используются для чего?**
   - `rax` - возвращаемое значение, также используется для вычислений
   - `rdi` - первый аргумент
   - `rsi` - второй аргумент
   - `rdx` - третий аргумент
   - `rcx`, `r8`, `r9` - следующие аргументы
   - `rsp` - указатель стека
   - `rbp` - указатель базы (обычно сохраняется)

3. **Что такое системные вызовы?**
   - Способ взаимодействия программы с ядром ОС
   - Номер системного вызова помещается в `rax`
   - Аргументы передаются в `rdi`, `rsi`, `rdx`, `r10`, `r8`, `r9`
   - Вызов через `syscall` (Linux) или `syscall` с макросом (macOS)

4. **Разница между macOS и Linux?**
   - **Формат объектных файлов**: macOS использует `macho64`, Linux использует `elf64`
   - **Системные вызовы**: macOS использует смещение `0x2000000` (write = `0x2000004`), Linux использует прямые номера (write = `1`)
   - **Имена функций**: macOS использует префикс подчёркивания (`_ft_strlen`), Linux без префикса (`ft_strlen`)
   - **Обработка ошибок**: macOS использует `___error()`, Linux использует `__errno_location()`
   - **Определение ошибок**: macOS использует флаг переноса (`jnc`), Linux проверяет отрицательное возвращаемое значение
   - **Makefile**: Автоматически определяет платформу и использует соответствующие флаги

### Вопросы о конкретных функциях

1. **Почему `ft_strlen` не проверяет `NULL`?**
   - Стандартная `strlen` также не проверяет `NULL` (undefined behavior)
   - Проверка добавляет overhead
   - Это соответствует поведению стандартной библиотеки

2. **Как работает `ft_strdup`?**
   - Вызывает `ft_strlen` для определения размера
   - Выделяет память через `malloc(strlen(s) + 1)`
   - Копирует строку через `ft_strcpy`
   - Возвращает указатель или `NULL` при ошибке

3. **Почему `ft_write` и `ft_read` возвращают `ssize_t`?**
   - `ssize_t` - знаковый тип, может быть -1 при ошибке
   - `size_t` - беззнаковый, не может быть отрицательным
   - Это соответствует стандартной библиотеке

---

## Демонстрация работы

### Подготовка демонстрации

1. **Соберите проект:**
   ```bash
   make fclean
   make bonus
   make test
   ```

2. **Подготовьте простые примеры:**
   ```c
   // Пример для ft_strlen
   printf("ft_strlen(\"Hello\") = %zu\n", ft_strlen("Hello"));
   
   // Пример для ft_strcpy
   char dst[100];
   ft_strcpy(dst, "Hello");
   printf("Copied: %s\n", dst);
   
   // Пример для ft_strcmp
   printf("ft_strcmp(\"abc\", \"abd\") = %d\n", ft_strcmp("abc", "abd"));
   ```

3. **Демонстрация списков:**
   ```c
   t_list *list = NULL;
   ft_list_push_front(&list, "third");
   ft_list_push_front(&list, "second");
   ft_list_push_front(&list, "first");
   printf("Size: %d\n", ft_list_size(list));
   ```

### Что показать на защите

1. ✅ Проект собирается без ошибок
2. ✅ Все тесты проходят
3. ✅ Функции работают корректно
4. ✅ Обработка граничных случаев (пустые строки, NULL и т.д.)
5. ✅ Понимание кода и алгоритмов

---

## Частые ошибки

### 1. Неправильное использование регистров
- ❌ Использование регистров, которые должны сохраняться (`rbx`, `rbp`, `r12-r15`) без сохранения
- ✅ Сохраняйте эти регистры в стек, если используете

### 2. Неправильная работа со стеком
- ❌ Несбалансированный стек (push без pop)
- ✅ Всегда балансируйте стек

### 3. Неправильные системные вызовы
- ❌ Использование неправильного номера системного вызова
- ✅ Проверьте номера для вашей ОС (macOS vs Linux)

### 4. Неправильная обработка указателей
- ❌ Потеря указателя на функцию при вызове
- ✅ Сохраняйте указатели на функции в безопасных регистрах

### 5. Неправильная работа с памятью
- ❌ Утечки памяти в `ft_strdup` и функциях списков
- ✅ Всегда освобождайте выделенную память в тестах

---

## Полезные команды

### Отладка

```bash
# Дисassembly объектного файла
objdump -d build/ft_strlen.o

# Просмотр символов
nm libasm.a

# Проверка архитектуры
file libasm.a
file tests/test_libasm

# Запуск под отладчиком
lldb tests/test_libasm
# или
gdb tests/test_libasm
```

### Тестирование

```bash
# Запуск тестов
make test

# Компиляция тестового файла вручную (macOS)
cc -arch x86_64 -Wall -Wextra -Werror -I include tests/main.c libasm.a -o test_manual

# Компиляция тестового файла вручную (Linux)
cc -Wall -Wextra -Werror -I include tests/main.c libasm.a -o test_manual

# Запуск ручного теста
./test_manual
```

### Команды для разных платформ

```bash
# Проверить текущую платформу
uname -s

# Просмотреть содержимое архива
ar -t libasm.a

# Проверить индекс архива (Linux)
ranlib libasm.a
# или
ar s libasm.a

# Просмотреть символы в архиве
nm libasm.a

# Дисassembly объектного файла
objdump -d build/ft_strlen.o  # Linux
otool -tV build/ft_strlen.o   # macOS
```

### Проверка кода

```bash
# Проверка нормы (если установлен norminette)
norminette src/

# Поиск проблем в коде
grep -r "TODO\|FIXME\|BUG" src/
```

---

## Протокол защиты проекта

### Пошаговый процесс защиты

1. **Начальная настройка (2-3 минуты)**
   ```bash
   # Показать чистое состояние
   ls -la
   make fclean
   
   # Собрать проект
   make bonus
   
   # Проверить компиляцию
   ls -la libasm.a
   ```

2. **Запуск тестов (2-3 минуты)**
   ```bash
   make test
   # Показать, что все тесты проходят
   ```

3. **Разбор кода (15-20 минут)**
   - Начните с обязательных функций
   - Объясните каждую функцию построчно
   - Покажите использование регистров и calling convention
   - Объясните системные вызовы для `ft_write` и `ft_read`

4. **Бонусные функции (если реализованы) (10-15 минут)**
   - Объясните операции со списками
   - Покажите управление памятью
   - Продемонстрируйте указатели на функции

5. **Вопросы и ответы (10-15 минут)**
   - Ответьте на вопросы о вашей реализации
   - Обсудите возможности оптимизации
   - Объясните различия платформ (macOS vs Linux)

### Советы для успешной защиты

1. **Тщательно изучите код**
   - Вы должны понимать каждую строку ассемблера
   - Знать, почему используется каждая инструкция
   - Понимать использование регистров и управление стеком

2. **Практикуйте свои объяснения**
   - Объясняйте код вслух самому себе
   - Практикуйте пошаговое отслеживание выполнения
   - Подготовьтесь к вопросам "что произойдет, если..."

3. **Будьте готовы к типичным вопросам**
   - "Почему вы использовали `xor rax, rax` вместо `mov rax, 0`?"
   - "Как работает обработка ошибок в `ft_write`?"
   - "В чем разница между реализацией для macOS и Linux?"
   - "Можно ли это оптимизировать?"

4. **Демонстрируйте уверенно**
   - Покажите, что все тесты проходят
   - Мысленно пройдитесь по выполнению кода
   - Объясните обработку граничных случаев

5. **Будьте честны и ясны**
   - Если чего-то не знаете, признайте это
   - Покажите, что вы понимаете основы
   - Объясните свой ход мыслей

---

## Типичные сценарии защиты

### Сценарий 1: "Покажите, как работает ft_strlen"

**Что делать:**
1. Откройте `src/ft_strlen.s`
2. Пройдитесь по каждой инструкции:
   - `xor rax, rax` - инициализация счётчика нулём
   - `.loop:` - начало цикла
   - `cmp byte [rdi + rax], 0` - сравнение текущего байта с нулевым терминатором
   - `je .done` - если равно, выход из цикла
   - `inc rax` - увеличение счётчика
   - `jmp .loop` - продолжение цикла
   - `.done: ret` - возврат длины в rax

### Сценарий 2: "Объясните системный вызов в ft_write"

**Что делать:**
1. Объясните механизм системного вызова:
   - Номер системного вызова в `rax`
   - Аргументы в `rdi`, `rsi`, `rdx`
   - Инструкция `syscall`
2. Покажите различия платформ:
   - macOS: `0x2000004` (со смещением)
   - Linux: `1` (прямой номер)
3. Объясните обработку ошибок:
   - Проверка возвращаемого значения (отрицательное = ошибка)
   - Установка errno через функцию, специфичную для платформы

### Сценарий 3: "Как работает ft_strdup?"

**Что делать:**
1. Покажите алгоритм:
   - Сохранить `rdi` в стек (он будет изменён)
   - Вызвать `ft_strlen` для получения длины
   - Выделить `length + 1` байтов через `malloc`
   - Проверить, вернул ли `malloc` NULL
   - Восстановить исходный указатель на строку из стека
   - Вызвать `ft_strcpy` для копирования строки
   - Вернуть указатель или NULL

## Дополнительные ресурсы

- [NASM Documentation](https://www.nasm.us/docs.php)
- [x86-64 Calling Convention](https://en.wikipedia.org/wiki/X86_calling_conventions)
- [System Call Numbers - macOS](https://github.com/apple/darwin-xnu/blob/main/bsd/kern/syscalls.master)
- [System Call Numbers - Linux](https://chromium.googlesource.com/chromiumos/docs/+/master/constants/syscalls.md)
- [Руководство по x86-64 Assembly](https://www.cs.virginia.edu/~evans/cs216/guides/x86.html)
- [Таблица системных вызовов Linux](https://syscalls.kernelgrok.com/)

---
